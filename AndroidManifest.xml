<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.clevelandbarscene.barapp"
      android:versionCode="1"
      android:versionName="1.0">
    <uses-sdk android:minSdkVersion="7" />
    <uses-permission android:name="android.permission.INTERNET" /> 
    <application android:icon="@drawable/icon" android:label="@string/app_name">
        <activity android:name=".ClevelandBarSceneActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
		<activity android:name=".Events"
                  android:label="@string/events"
                  android:theme="@android:style/Theme.NoTitleBar">
        </activity>
        <activity android:name=".Venues"
                  android:label="@string/venues"
                  android:theme="@android:style/Theme.NoTitleBar">
        </activity>
        <activity android:name=".Venue"
                  android:label="@string/venue"
                  android:theme="@android:style/Theme.NoTitleBar">
        </activity>
        <activity android:name=".Webcam"
                  android:label="@string/webcam"
                  android:theme="@android:style/Theme.NoTitleBar">
        </activity>
    </application>
</manifest>